services:
  - type: web            # веб-сервис для keep_alive
    name: husenov-ff-bot # имя сервиса (маленькие буквы и дефис)
    env: python
    region: frankfurt
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
