services:
  - type: web
    name: husenov-ff-bot      # маленькие буквы через дефис
    env: python
    region: frankfurt
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python bot.py"
